# AGENTS.md

## 教程写作规范

- 标题要简洁，优先让读者一眼看懂主题。
- 正文少 AI 味，避免空话、套话、翻译腔和过度解释。
- 使用中文文章标点，中文段落中不要混用英文逗号、句号。
- 段落保持紧凑，普通段落之间尽量少留空行；标题、图片、表格、列表和代码块前后保留必要空行。
- 文章以实用为主，减少重复介绍；同一概念已经在其他文档讲过时，只保留必要引用和核心结论。
- 删除填充短语，少用“可以理解为”“简单说”“需要注意的是”等解释腔。
- 避免金句式结尾，结尾优先给具体动作或使用场景。
- 少用三段式排比，能用两项或四项就不要强行凑三项。
- 长句优先拆短，具体信息优先于抽象判断。
- 图片较多时，减少重复表格和重复说明。
- 正文不使用中文全角引号；需要突出时使用 Markdown 加粗。
- 正文不使用 Markdown 分割线；不设置发布前检查、参考资料、参考文献、资料来源或延伸阅读等区块。真实性核验在写作过程中完成，正文只保留经核验的结论与必要边界。
- 标题默认不使用问号；只有用户明确要求保留时才使用。

## 图片规范

- 配图优先使用官方图标、官方界面图或用户提供的真实素材；工具教程和开源项目教程优先使用中文界面截图。
- 如果产品支持中文界面，截图前先切换中文语言或使用官方中文界面图；如果官方只提供英文界面，可使用官方原图，但正文必须用中文解释关键菜单、按钮和功能名。
- 没有官方素材时，再做自定义设计图；自制 UI 图必须使用中文界面文字，不得伪造官方产品界面。
- 自定义图要保持中文可读、颜色对比清晰、布局不遮挡文字。
- 文章封面默认使用 2.35:1 横版，推荐 2350 × 1000 px；左侧主副标题、右侧主题视觉主体，深色科技基底，配色必须随文章主题调整。
- 封面先生成原创视觉底图，再用确定性排版叠加并校对中文主副标题；禁止使用错字、乱码、伪造品牌 Logo、伪造官方界面或与文章无关的通用画面。
- 封面提示词与动态配色规则以 `09-工具脚本/封面提示词模板-V1.3.md` 为准；V1、V1.2 仅作历史参考。每累计 10 张封面后，根据反馈和表现复盘优化版本。
- 后续新增或重写文章统一按 `07-资料与流程/后续文章生成方案.md` 执行：先 Brief，再正文，再外置登记元信息，再配图，再入库索引，再发布记录；如果用户只要草稿，明确不入库不提交。
- 提交前检查所有 Markdown 图片引用，确保文件存在且显示位置合理。
- 后续新增、整合或重写文章，正文必须至少配置 1 个阅读缓冲模块：优先使用本地正文插图；如果暂不配图，必须改用“视觉速读 / 方法卡 / 对比卡 / 检查清单 / 流程卡”等可扫读版式，避免整篇都是连续纯文本。
- 正文插图不得放在首行，也不得复用封面图；推荐放在一句话结论之后、第一组正文小标题之前，用于帮助读者先抓住文章结构。

## article-wechat Skill 与统一生产流程

- 通用文章生产标准仅以私有仓库 `article-Skill` 为准；本项目通过 `09-工具脚本/article-skill.lock.json` 锁定版本。`09-工具脚本/article-wechat-skill/` 为历史快照，只读保留，不再维护。
- 新需求先按任务路由选择选题、写作、改稿、配图、封面、入库或发布流程；再按交付风险选择快速稿、标准稿或深度稿。标准稿与深度稿必须保留单篇元数据记录。
- 封面固定采用两阶段生产：模型只生成无文字的主题视觉底图，本地确定性排版叠加中文标题、能力标签、控件与连接线，并完成可读性校对。
- 统一检查入口为 `09-工具脚本/文章统一检查.py`：它按文章编号读取元数据清单，并调用锁定版本的通用检查器。

## 外部原文归档与内容去重

- 外部文章用于后续查重时，归档到 `10-外部原文归档/EXT-日期-序号-标题/`，不得占用正式文章编号、文章资产登记表或主 README 的正式文章表。
- `source.html` 是不可人工编辑的原始快照；`content.md` 和 `content.txt` 只能由原始 HTML 自动转换，禁止把外部原文改写后当作正式文章。
- 新增或重写正式文章前，先运行 `node 09-工具脚本/重建内容库索引.mjs`，再运行 `node 09-工具脚本/检查内容重复度.mjs --candidate <候选文件>`。
- 重复度公式为“正文五字片段 Dice 相似度 × 85% + 标题二字片段 Dice 相似度 × 15%”；所有非豁免候选必须**低于 50%**。规范化标题或正文完全一致时直接阻止。
- 检查结果为“人工复核”时，必须在选题或发布记录中说明新增角度；结果为“阻止”时，不得入库为新文章。

## 开源项目文章生产流程

- 写开源项目公众号文章时，默认先核对 GitHub README、官方文档和核心安装/安全页面，资料时间写入内部核验记录。
- 正文至少覆盖项目定位、新手快速上手、核心功能、可复现使用案例、适用边界和新手避坑。
- 插入官方 UI 截图时，同一段落必须补充基础功能介绍，说明读者看到的主要区域、入口、按钮或能力边界。
- 配图默认包含 1 张封面和至少 2 张正文信息图；正文图要解释架构、流程或案例，不复用封面；产品界面图默认优先中文界面。
- 使用案例必须标注为可复现模板或已验证经历，不虚构客户案例、效率数据或生产经验。

## 提交前检查

- 标准稿和深度稿在发布、推送或完成知识库维护前，先运行 `python 09-工具脚本/文章统一检查.py --id <文章编号> --strict`；再运行 `python 09-工具脚本/一键发布检查.py`。后者会串联索引重建、文章质量扫描、选题绑定、图片资产、本地链接、数量一致性、Git 格式、删除项和敏感信息检查。
- 检查 Markdown 链接、图片引用和代码块是否完整。
- 检查是否存在乱码、明显错别字和过时文件名。
- README 有新增文章或素材时，需要同步更新目录和图片说明。

## 选题阶段追踪

- 新增或重写文章前，先登记或更新 `07-资料与流程/选题库.md`，并对照 `07-资料与流程/已发布文章去重登记.md` 做查重；不得只换标题重复旧文。
- 每启动一篇后续文章，先把对应选题从 `S0 灵感池` 更新到 `S1 已立题`；后续按 `S2 已做资料/Brief`、`S3 初稿中`、`S4 待质检`、`S5 待发布`、`S6 已入库`、`S7 已复盘` 逐步记录，不跳过状态登记。
- 如果选题暂缓、合并或放弃，分别标记为 `HOLD` 或 `DROP`，并写清原因、恢复条件或合并去向。
- 如果选题与已发布文章高度重叠，必须在选题库写清新增角度：新读者、新场景、新案例、新模板、新资料或团队版；否则不进入正文写作。
- 进入正文前必须先形成 Brief，至少包含目标读者、核心问题、不写什么、核心观点、交付物、资料要求、风险点和关联旧文。
- 正式入库或推送后，同步更新 `选题库.md`、`已发布文章去重登记.md`、状态跟踪文件、总索引、分类索引、系列索引、标签索引和发布记录，确保主题、阶段、编号、路径和 GitHub 状态可追溯。

## 选题库与同步管控

- 选题库、已发布去重登记和同步管控机制分别以 `07-资料与流程/选题库.md`、`07-资料与流程/已发布文章去重登记.md`、`07-资料与流程/选题同步与规划管控机制.md` 为准。
- `main` 作为个人生产与规划工作区，`prod` 作为确认后的共享知识库展示区；后续项目修改默认先落 `main`，用户确认后再同步 `prod`。
- 同一 GitHub 仓库的不同分支不是隐私边界；真正私密内容不得放入公开仓库或公开分支历史。
- 每次新增选题、发布文章或同步分支后，都要能从选题库、资产登记表、发布记录和 Git 状态追溯来源与去向。

## 文章编号与上传

- 后续新增或重写文章统一按 `07-资料与流程/后续文章生成方案.md` 执行：先 Brief，再正文，再配图，再入库索引，再发布记录；如果用户只要草稿，明确不入库不提交。
- 正式文章使用两位数编号，新稿使用当前最高正式编号加 1。下架、删除或转为草稿不自动重排；仅当负责人明确批准“一次性编号迁移”时，才按 `07-资料与流程/编号变更记录.md` 中的映射执行。
- 新增配图必须使用“文章编号-中文文章标题-用途”命名，且标题与 `07-资料与流程/文章资产登记表.md` 中的正式标题一致：封面为 `NN-中文文章标题-封面.扩展名`，正文插图为 `NN-中文文章标题-正文插图01.扩展名`；封面放入 `08-素材库/图片/文章封面/`，正文插图放入 `08-素材库/图片/正文插图/`。重命名时必须同步更新全部本地引用、README 与知识库索引。
- 新增文章时同步更新 `README.md`、`00-知识库导航/知识库总索引.md`、`分类索引.md`、`系列索引.md`、`标签索引.md`、`07-资料与流程/文章资产登记表.md` 和 `07-资料与流程/发布记录.md`；默认使用文件名前缀编号。删除、下架或转为草稿不修改既有文章文件名、图片和元数据；只有已批准的一次性迁移，才按《编号变更记录》同步这些内容。
- 更新 README、首页或知识库索引后，必须运行 `09-工具脚本/重建知识库索引.py` 或按其输出规则重建，确认展示编号与文章文件名前缀一致、链接可访问、资产登记表、元数据和正在使用的素材文件均已同步，需要时可依《编号变更记录》追溯。
- 更新 README、首页或知识库索引后，运行 `09-工具脚本/重建知识库索引.py`，确认展示编号与文件名前缀一致、链接可访问；批量重排时另行检查文件、正在使用的图片、元数据和映射记录已同步。
- 分类文章下架、迁移或资产登记表删除某分类最后一篇文章时，也必须重建对应分类 `README.md`；空分类 README 应显示“暂无正式入库文章”，不得保留旧文章表格、旧链接或缺失图片引用。

### 知识库维护与查阅

- 读者首次进入知识库时，优先从 `00-知识库导航/阅读路径.md` 按目标开始；分类、系列、标签和总索引只负责查全，不重复维护阅读顺序说明。
- 正式文章的编号、标题、分类、系列、标签、路径、字数和入库状态，以 `07-资料与流程/文章资产登记表.md` 为当前汇总索引；标准稿和深度稿另以 `07-资料与流程/文章元数据/NN-中文标题.json` 记录单篇素材、来源、封面与检查结果。选题状态、发布记录、图片记录和下架记录分别按 `07-资料与流程/更新工作流.md` 的分工维护。
- 下架或合并文章时，必须更新 `07-资料与流程/下架文章与替代关系.md`；历史发布编号保留在记录中，需要批量重排时以 `编号变更记录.md` 为准。
- 新增、替换或清理图片后，运行 `09-工具脚本/图片资产检查.py`；正文插图必须被正式文章引用，封面必须与正式文章编号匹配，未归属或已下架编号残留的图片不得直接忽略。
- 仓库文本文件使用 UTF-8 与 LF 换行；编辑器行为以根目录 `.editorconfig` 和 `.gitattributes` 为准。

- 除非任务明确要求“仅草稿”或“不上传”，文章和素材完成本地校验后提交并推送 `main`；推送前先获取远端更新，禁止强推。


## main 到 prod 晋级发布规则

- 默认所有项目修改先在 `main` 完成：本地校验通过后，提交并推送 `main`，不得直接把未确认改动提交到 `prod`。
- `prod` 只作为确认后的稳定发布分支；只有用户明确确认“同步到 prod”“发布到 prod”或同等表达后，才把 `main` 中已确认的提交合并或同步到 `prod`。
- 同步 `prod` 前先确认 `main` 与 `prod` 的差异，优先保留 `prod` 已有的知识库导航、发布记录等稳定规则，再合入 `main` 的最新内容。
- 同步 `prod` 后必须至少完成文章质量扫描、Markdown 链接/图片检查和 Git 格式检查；检查通过后再推送 `prod`。
- 同步完成后向用户说明：`main` 提交、`prod` 提交、检查结果、是否已推送，以及当前工作区是否仍有未提交文件。

## 知识库目录约定

- 顶层只保留 GitHub/Codex 必需入口文件（`README.md`、`AGENTS.md`）和中文一级目录。
- 文章按主分类进入中文目录；交叉分类通过索引和标签解决，不复制正文文件。
- 素材、脚本、流程文档分别放入 `08-素材库/`、`09-工具脚本/`、`07-资料与流程/`；不保留整包发布归档，只保留正式文章实际引用的图片素材。

## 文章质量与微信发布兼容规则

- 正式文章正文只保留可发布内容，不展示元信息表、frontmatter 或隐藏元数据块。
- 正文不重复文章标题，不使用 H1；开头默认使用 `> 一句话结论：……`。
- 封面图外置到素材库和文章资产登记表，不作为正文第一张图重复展示。
- 每篇正式文章至少保留 1 个图文或视觉化版式模块；优先使用 `../08-素材库/图片/正文插图/编号-文章标题-正文插图01.png`，没有图片时必须用卡片、清单、对比块或流程块承接。
- 加粗必须克制：每篇建议不超过 6 处，超过 10 处发布前必须修复；禁止 `****`、相邻加粗、整句加粗和单字加粗。
- 微信公众号发布版原则上不保留围栏代码块；命令、提示词、模板和示例改成引用示例卡、编号清单或静态图片。Mermaid 必须转为静态图片后引用；确需代码教程时，发布前单独确认转换器支持。
- 正文不得保留 `[^1]` 这类脚注标记；需要来源时，改为正文内有边界表达，并把来源、访问日期、版权核验写入资产登记表或发布流程。
- 宽表格优先改成卡片、小节或编号清单；4 列及以上表格默认视为移动端 P1 问题。
- 不把“发布前请复核”“审核记录”“图片来源/参考资料”等内部流程区块放进公开正文；公开稿只可保留统一收束句：`本文发布前已完成事实、版权与格式检查。`
- 对没有点名来源的产品趋势，不写“当前一些产品……”这类过实判断；改成“许多 AI 写作/项目工具正在……”等类别表达。
- 结尾使用自然收束或下一步行动建议，不在知识库正文中写“点在看”“关注公众号”等平台 CTA。
- 发布前参考 `07-资料与流程/文章质量检查与微信发布规范.md`，并运行 `09-工具脚本/文章质量扫描.py` 做 P0/P1 检查。

## 后续任务自动提交约定

- 用户已确认：后续本知识库任务默认在本地校验通过后提交并推送到 `origin/main`，除非用户明确要求“仅本地修改”“仅草稿”“不要提交”或“不要推送”。
- 自动提交前必须先获取远端 `main` 最新状态，确认没有未处理的远端更新或冲突；禁止强推。
- 提交说明要简洁写清本次文章、索引、素材或流程变更范围；提交后向用户说明提交哈希、推送分支和本地是否仍有未提交文件。


## 正文图片视觉质量规范

- 正文图生成前必须先写图片 Brief：明确图片目标、文章类型、图片类型、内容结构、视觉风格、中文文案和禁止元素；不得直接套固定皮肤。
- 正文信息图默认优先 1400 × 820 px 或 16:9；一张图只表达一个流程、判断、结构或执行清单，核心节点不超过 6 个，按 375 px 手机宽度预览仍要可读。
- 风格根据文章内容变化：工具教程偏清爽产品说明风，Agent 实战偏工程工作流风，AI 知识偏教学白板风，案例复盘偏项目看板风，安全治理偏风险控制风，好文方法偏杂志卡片风，选题趋势偏轻数据报告风。
- 禁止廉价科幻风正文图：不要大面积深色霓虹、发光边框、宇宙粒子、复杂电路线、随机英文、乱码、伪官方界面、真实 Logo、二维码、人形 AI 或机器人头像。
- 正文图不放无信息量的装饰性胶囊标签、品牌角标或悬浮片头；步骤和状态图标必须语义明确，禁止减号、随机连线、伪 Logo 或弱符号冒充功能图标。
- 正文图中文必须用确定稿并校对；如用图片模型生成底图，中文标题和说明优先用本地脚本、SVG 或设计工具叠加，避免模型生成错字。
- 发布前检查布局完整性：标题不裁切、卡片不贴边、箭头不穿字、文字不重叠、底部结论不压线、图片路径正确且已被正式文章引用。
- 正文图必须检查视觉节奏：主标题与副标题之间、模块标题与说明之间、卡片标题与说明之间都要有明显呼吸感；不得出现上下行贴近、压线、挤成一团的观感。
- 新生成或重绘正文图必须额外运行 `python 09-工具脚本/图片视觉检查.py --focus <图片文件名> --strict`，并打开 `.tmp/图片视觉检查预览.jpg` 做 375px 手机宽度人工复核；全仓发布检查默认运行 `图片视觉检查.py` 生成预览和提醒。
## 正式文章编号维护（2026-07-23）

- 默认只递增：当前正式文章为 01–18，下一篇新稿使用 `19`。下架、删除或转为草稿时，不自动重排以及不批量更名已发布文章。
- 只有负责人明确批准“一次性编号迁移”时，才允许重排。先在 `07-资料与流程/编号变更记录.md` 固化旧号→新号映射、涉及资产和验收标准，再执行。
- 一次性迁移必须同步正文、正在使用的图片、正文引用、元数据、资产登记表、README/索引、本地链接和发布记录，然后运行 `python 09-工具脚本/一键发布检查.py`。
- `07-资料与流程/文章资产登记表.md` 同时记录「入库状态」和「发布状态」；正式文章上线、修改或下架时先更新发布状态，再运行 `09-工具脚本/重建知识库索引.py` 同步 README 的每篇文章状态列。
