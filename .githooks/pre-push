#!/bin/sh
set -eu
if [ "${CODEX_SKIP_PRE_PUSH:-0}" = "1" ]; then
  echo '[pre-push] skipped because CODEX_SKIP_PRE_PUSH=1'
  exit 0
fi
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$(git rev-parse --show-toplevel)/scripts/verify.ps1"
