# main 与 prod 分支基线审计

> 审计日期：2026-08-21
> 仓库：`D:\projects_git\Codex_article`
> 远端：`origin`（GitHub codex-article）

## 当前事实

| 检查项 | 结果 |
| --- | --- |
| 当前工作分支 | `main` |
| `main` 与 `origin/main` | 一致，ahead 0 / behind 0 |
| `prod` 与 `origin/prod` | 一致，ahead 0 / behind 0 |
| `main` 相对 `prod` | main 多97个提交，prod 多6个提交 |
| 是否执行合并 | 否 |
| 是否执行强推 | 否 |

## 判断

`main` 与 `prod` 已经不是简单的单向落后关系，而是发生了双向历史分叉。不能直接把 `main` 覆盖到 `prod`，也不能通过强制推送解决。prod 中的6个独有提交必须先判断是否包含稳定版导航、发布记录或其他必须保留的规则。

## 建议的晋级动作

1. 导出 `main` 与 `prod` 的提交差异及文件差异。
2. 对prod独有的6个提交逐项标注“保留、迁移、废弃”。
3. 形成一次明确的合并基线，再通过普通合并或人工挑选提交完成晋级。
4. 晋级后运行文章统一检查、图片资产检查、Markdown链接检查、项目深度验证和Git格式检查。
5. 后续保持 `main` 生产、`prod` 稳定展示的单向晋级关系。

## 当前发布结论

本次只完成分支审计，没有修改或同步 `prod`。当前可安全同步的目标是 `main`；`prod` 仍需单独确认分支治理方案。
