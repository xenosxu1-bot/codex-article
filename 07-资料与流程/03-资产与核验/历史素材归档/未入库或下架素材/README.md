# 素材库

本目录只保留正式知识库需要长期引用的素材。

## 当前规则

- 不再保留整包发布归档。
- 正式文章实际引用的图片保留在 `图片/` 下。
- 临时生成的公众号复制版、源包、压缩包、质检包等不进入长期仓库；如需保留，应先确认是否具有复盘价值。

## 目录

```text
图片/
  文章封面/   文章 隐藏元信息块 `cover` 使用的封面图
  正文插图/   正文中使用、但不作为封面的配图
```

新增图片后，请使用“文章编号-中文文章标题-用途”的中文标题命名，同步更新文章中的相对路径，并执行链接检查。