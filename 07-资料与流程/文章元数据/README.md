# 文章元数据

每个标准稿或深度稿使用一个 `NN-中文标题.json` 记录文章路径、封面、正文图片、来源、生产层级、检查状态和发布状态。

## 使用规则

1. 新建或重写标准稿、深度稿时，同步创建或更新对应 JSON。
2. 图片路径必须与正文 Markdown 引用、素材库中的真实文件一致。
3. 发布前执行 `python 09-工具脚本/文章统一检查.py --id <文章编号> --strict`。
4. `文章资产登记表.md` 继续承担知识库的汇总索引职责；本目录用于单篇可验证记录。
